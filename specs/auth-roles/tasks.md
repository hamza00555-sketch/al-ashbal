# Auth & Roles — Tasks

1. Define UserRole TypeScript type.
2. Create mock users.
3. Create helper functions:
   - getCurrentMockUser
   - canViewChild
   - canViewRecitation
   - canViewWish
4. Use helpers in pages even if data is mock.
