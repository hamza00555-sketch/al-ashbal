# قائمة جاهزية الإطلاق — الأشبال

آخر تحديث: 2026-07-04 (المرحلة 3). ✅ = منجز ومتحقق منه · ⬜ = على المالك.

## البنية والأمان

- ✅ مصادقة حقيقية (معلم/ولي أمر) + حماية خادمية لكل المسارات المحمية.
- ✅ طلبات انضمام بموافقة معلمة (لا تسجيل مفتوح).
- ✅ دعوات عائلات + تسجيل أطفال (ليسوا مستخدمي Auth) + منح أجهزة (هاش فقط).
- ✅ خط التسميع كاملًا: تسجيل ← رفع لدلو خاص بروابط موقعة ← موافقة الوليّ
  ← مراجعة المعلمة ← نقاط. حماية الأشقاء بقيد قاعدة البيانات.
- ✅ 12 هجرة مطبقة، RLS على كل الجداول، صفر صلاحيات anon، بوابة أمان
  موثقة (docs/SECURITY_LAUNCH_GATE_REPORT.md).
- ✅ أداء: أصول مضغوطة 99%، حالات تحميل فورية، استدعاءات auth مخفضة.

## على المالك قبل أول عائلة حقيقية

- ⬜ تدوير مفتاح sb_secret (docs/SECRET_ROTATION_BEFORE_LAUNCH.md).
- ⬜ تفعيل Leaked Password Protection (لوحة Supabase ← Auth ← Passwords).
- ⬜ التأكد أن Site URL = https://al-ashbal.vercel.app (روابط البريد).
- ⬜ تنفيذ سيناريو البايلوت الحقيقي (docs/PRODUCTION_PILOT_REPORT.md)
  مع عائلة واحدة قبل التوسع.
- ⬜ (اختياري موصى به) ضبط CHILD_GRANT_TOKEN_PEPPER في Vercel.

## معروف ومقبول للإطلاق المغلق

- تجربة الطفل التفصيلية (مهام الديمو/الأوسمة/الأنشطة) ما زالت محلية —
  موسومة، وخط التسميع الحقيقي منفصل وواضح.
- صوت فقط في واجهة التسجيل؛ لا إشعارات بريدية؛ لا مؤشر نسبة رفع.
- إضافة جهاز ثانٍ للطفل تأتي لاحقًا (أكواد وصول الطفل).

## بعد الإطلاق (مقترح بالترتيب)

1. أكواد وصول الطفل لجهاز ثانٍ + إدارة المنح للوليّ.
2. ترحيل الإشعارات + إشعارات بريد/دفع للأحداث المهمة.
3. تنظيف تسجيلات rerecord القديمة من التخزين + مؤشر نسبة الرفع.
4. لوحة تقدم/تقارير للمعلمة (child_progress).
5. إزالة خط الديمو المحلي نهائيًا بعد ثبات الاستخدام الحقيقي.
