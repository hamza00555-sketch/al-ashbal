# تقرير بوابة الأمان قبل الإطلاق — Security Launch Gate

**التاريخ:** 2026-07-03 · **الفرع:** `claude/pensive-hypatia-k6qnf3`
**الحكم النهائي: ✅ جاهز للإطلاق المغلق — بشرطين على المالك** (انظر «شروط
الإطلاق» أدناه: تدوير sb_secret + تفعيل حماية كلمات المرور المسربة).

## 1) الأسرار

| الفحص | النتيجة |
| --- | --- |
| لا `.env`/`.env.local` في git (الوحيد المتتبع: `.env.example` بلا قيم) | ✅ |
| لا `sb_secret_` في أي كود/وثيقة متتبعة | ✅ |
| لا مفتاح خدمة في حزمة المتصفح (فحص chunks: صفر تطابق) | ✅ |
| endpoint التشخيص `/api/health/supabase` محذوف (لا يوجد مجلد api أصلًا) | ✅ |
| السجلات الخادمية تطبع أسماء أخطاء وbooleans فقط (لا قيم/رموز) | ✅ |
| مفتاح الخدمة على Vercel: **موجود** (تحقق سلوكي — الموافقات على الطلبات تعمل على الإنتاج، وهي تمر حصرًا عبر مفتاح الخدمة؛ القيمة نفسها لم تُقرأ ولن تُطبع) | ✅ |

## 2) Supabase / RLS (فحوص حية على المشروع)

| الفحص | النتيجة |
| --- | --- |
| الهجرات المطبقة | ✅ **12/12** (001–012) |
| جداول public بلا RLS | ✅ **0** (كل الجداول RLS-on، 40 سياسة) |
| صلاحيات anon على جداول public | ✅ **صفر** grants |
| صلاحيات authenticated | ✅ DML فقط (SELECT/INSERT/UPDATE/DELETE) — RLS هو الحاكم |
| bucket التسجيلات `recordings` | ✅ خاص (public=false) |
| محاكاة مستخدم موثق (أسماء عبر JWT claims): ترى الطلبات، is_app_teacher=true، وملفها فقط من profiles | ✅ |
| anon على دوال SECURITY DEFINER | ✅ **أُغلقت في هذه البوابة** — migration 012 (كان anon يرث EXECUTE عبر PUBLIC رغم سحب 009؛ سُحب PUBLIC ومُنح authenticated صراحةً، وتحقق: `has_function_privilege('anon',...)=false` والسياسات تعمل) |
| Advisors المتبقية | مقبولة عمدًا: `audit_events` بلا سياسات (deny-all مقصود) · تنفيذ authenticated للدوال المساعدة (تصميم — لا تكشف إلا ما تتيحه RLS أصلًا) · «Leaked password protection معطلة» ← **بند على المالك** (Dashboard ← Auth ← Passwords، دقيقة واحدة) |

## 3) المسارات (QA فعلي بمتصفح على بناء إنتاجي + Supabase وهمي)

| الفحص | النتيجة |
| --- | --- |
| /teacher/* بلا جلسة ← تحويل /login (307 من الخادم) | ✅ |
| جلسة TCH-001 القديمة في localStorage لا تمنح شيئًا وتُمسح | ✅ |
| ولي أمر على /teacher ← «هذا الحساب ليس حساب معلم» بلا أي تسريب | ✅ |
| طلب معلّق على /teacher ← «طلبك قيد المراجعة» بلا تسريب | ✅ |
| طلب مرفوض على /teacher ← «تم رفض طلبك» بلا تسريب | ✅ |
| /login يوجّه حسب الدور (معلم←اللوحة، ولي أمر←/parent، بلا دور←رسالة) | ✅ |
| /join/request ينشئ طلبًا معلقًا فقط (صفر صلاحيات؛ الكتابة خادمية حصرًا) | ✅ |
| /teacher/join-requests تتطلب معلمًا (RLS + requireTeacher في الأفعال) | ✅ |
| لا endpoints إدارية مفتوحة (لا مجلد api؛ الأفعال كلها خلف requireTeacher) | ✅ |
| ديمو /join و/child و/parent المحلي يعمل كما هو | ✅ |
| فتح درج الأدوات = صفر استدعاءات خلفية (عاصفة prefetch مغلقة) | ✅ |

## 4) الخصوصية

| الفحص | النتيجة |
| --- | --- |
| لا سرد عام لطلبات الانضمام (anon صفر وصول للجدول) | ✅ |
| تعداد البريدات: «لديك طلب سابق قيد المراجعة» يكشف وجود طلب لنفس المرسل فقط — مقبول ومقصود في الـ spec | ✅ |
| بيانات الأطفال: لا شيء منها في الباك-إند بعد (localStorage محلي فقط)، وجداولها المستقبلية RLS-deny | ✅ |
| لا أدوات session replay ولا مراقبة خارجية (تحذير الخصوصية موثق في تقرير التنقل) | ✅ |

## 5) أداء المصادقة (الخطوة 1 — أُنجزت في 706a427)

18 استدعاء (16 getUser) لكل تنقّل ← **6** (عمل الطلب نفسه = 4) ·
getUser: ‏16 ← 2–3 · فتح الدرج: ~10–16 ← **0** · الاستجابة للضغطة:
‏98–127ms مخنوقًا ×4. التفاصيل في رسالة commit ‏706a427.

## 6) شروط الإطلاق على المالك (قبل أي عائلة حقيقية)

1. **تدوير sb_secret** — الدليل خطوة-بخطوة: `docs/SECRET_ROTATION_BEFORE_LAUNCH.md`.
2. **تفعيل Leaked Password Protection** — Supabase Dashboard ← Authentication
   ← Passwords ← تفعيل الفحص ضد HaveIBeenPwned.
3. (تذكير قائم) التأكد أن Site URL في إعدادات Auth يشير إلى
   `https://al-ashbal.vercel.app` (لروابط البريد).

## 7) خارج نطاق هذه البوابة (المرحلة القادمة)

دعوات العائلات الخلفية، ترحيل الأطفال/التسليمات/التسجيلات — لكل منها بوابة
أمان خاصة عند بنائها (خصوصًا مسار الطفل/الجهاز المحكوم من الخادم H1).
