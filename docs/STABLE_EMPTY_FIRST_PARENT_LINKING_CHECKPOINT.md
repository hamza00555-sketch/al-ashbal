# نقطة تثبيت مستقرة — Empty-First + Parent Linking

**Stable checkpoint** قبل الانتقال إلى المرحلة التالية.

## معلومات النقطة

- **الفرع (Branch):** `claude/pensive-hypatia-k6qnf3`
- **HEAD:** `e9c6a92846d2baf37c8af1c218b470117bd1d537` (`e9c6a92`)
- **الوسم المحلي (Local tag):** `stable-empty-first-parent-linking`
- **التاريخ:** 2026-06-20
- **Build:** ✅ ناجح (exit 0)
- **Lint:** ✅ ناجح (exit 0)

## ملخص ما تم التحقق منه (كله ناجح)

تم تنفيذ بوابة QA شاملة من البداية للنهاية (`Final-QA-Gate-Before-Next-Phase-01`)
بدون تعديل أي منطق منتج — تحقق فقط. النتائج:

### A — الأساس
- وجود commits: empty-first (`cf2fcdd`)، parent linking (`73bb08a`)،
  Arabic name auto-fit (`e9c6a92`).
- Build = 0، Lint = 0.

### B — الحالة الفارغة (Fresh empty-state)
بعد مسح `localStorage` و `IndexedDB`، كل الصفحات التالية تعرض حالة فارغة
صحيحة بلا أي بيانات تجريبية تلقائية وبلا أخطاء console:
`/child`, `/child/tasks`, `/child/progress`, `/child/lessons`,
`/parent`, `/parent/children`, `/parent/approvals`,
`/teacher`, `/teacher/materials`, `/teacher/children`,
`/teacher/attendance`, `/teacher/reviews`, `/teacher/prep`.
- كرت اليوم في `/child` يعرض زرّي «تفاصيل درس اليوم» و«حضور الحلقة» —
  وليس «عرض المهام».
- لوحات المعلم وولي الأمر تعرض أعدادًا حيّة = 0 فقط.

### C — ربط ولي الأمر بالطفل
لا أطفال → ربط طفل عبر `/parent/link-child` → يظهر في `/parent`
و`/parent/children` مع زر «إلغاء الربط» → إلغاء الربط يخفيه دون حذف الملف →
إعادة الربط تعمل.

### D — مزامنة ملف الطفل
تغيير اسم الطفل عبر `/settings?role=child` ينعكس فورًا في `/child`،
`/parent`، `/parent/children` لأن الـ override مخزّن حسب `childId`.

### E — ضبط الاسم العربي تلقائيًا (موبايل 390px)
- كلمة واحدة طويلة (`عبدالرحمنعبدالقادرعبداللهالعيدروسالحضرمي`): تظهر **كاملة**،
  بلا `…`، بلا قص، بلا كسر للكلمة — يتقلّص الخط حتى تتّسع (≈7.5px،
  `scrollWidth ≤ clientWidth`)، وزر الإعدادات يبقى ثابتًا.
- اسم متعدد الكلمات: يلتف بشكل طبيعي (`word-break: keep-all`).
- `/teacher` و`/child` غير مكسورين.

### F — بناء التدفّق الحقيقي من الصفر
مادة «القرآن» + درس «الفاتحة» → تحضير اليوم «القرآن · الفاتحة» (قابل للتعديل) →
كرت اليوم في `/child` يعرض «القرآن · الفاتحة» مع الزرّين الصحيحين →
`/child/lessons` (كروت مدمجة + بوابة كبيرة) → مهمة من «القرآن · الفاتحة» تظهر
في `/child/tasks` ضمن قسم «مهام من المعلم» منفصلة عن درس اليوم.

### G — التسجيل/الموافقة/المراجعة/النقاط
الطفل يسجّل → ولي الأمر يوافق → المعلم يقبل (+5) →
- النقاط ارتفعت من 0 إلى 5.
- تقدّم القرآن ارتفع من 0% إلى 25%.
- لا أوسمة وهمية (تبقى «واصل لتفتح أوسمة جديدة»).
- لا عناصر pending عالقة بعد الاكتمال.
- أعداد اللوحات حيّة فقط.

### H — Regression
- استقلال إعدادات كل دور (معلم/ولي أمر/طفل) — لا تتداخل.
- الطفل المرتبط يعرض ملفه المحدّث.
- المواد: تعديل + «إخفاء من التقدّم» + أرشفة/استعادة.
- التحضير: تعديل محفوظ (حفظ التعديل).
- لا مهام/طلبات/مراجعات تجريبية تلقائية.

## الحدود المعروفة (Known Limitations)

- **تجريبي عبر `localStorage` فقط** — لا قاعدة بيانات حقيقية.
- **لا يوجد backend ولا مصادقة (Auth)** حتى الآن.
- **لا حسابات حقيقية** — تبديل الأدوار تجريبي وليس تسجيل دخول.
- **لا مزامنة بين الأجهزة** — كل البيانات محلية على الجهاز/المتصفح.
- تسجيلات الوسائط محفوظة في `IndexedDB` على الجهاز فقط.

## ملاحظة عن الفرع

العمل المُتحقَّق منه بالكامل موجود على `claude/pensive-hypatia-k6qnf3`
(الفرع الوحيد على الـ remote والذي يحتوي على كل commits المشروع). هذه النقطة
تُثبّت تلك الحالة المستقرة.
